# Copy to .env.local and fill in. Never commit .env.local.

# Base URL of the CRM API. In local development the app derives the tenant
# subdomain from the browser host, so http://localhost:8000 is enough.
NEXT_PUBLIC_API_URL=http://localhost:8000
